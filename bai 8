#include<iostream>

using namespace std;

void round1(double n)
{
	int k = floor(n);
	if (n - k >= 0.50) cout << k + 1 << endl;
	else cout << k << endl;
}

void round2(double n)
{
	int k = round(n);
	n = k;
	cout << n << endl;
}

int main()
{
	double n;
	cin >> n;
	round1(n);
	round2(n);
}
